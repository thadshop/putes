#!/usr/bin/env bash

source "${HOME}/lib/bash/handle_ssh_agent.bash"
handle_ssh_agent -v
source "${HOME}/opt/pyvenv/bin/activate"
export PYTHONPATH="${HOME}/opt/python"
python "${HOME}/bin/sendmail.py" "${@}"
