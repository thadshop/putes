#!/usr/bin/env bash

typeset -r DEVICE_LISTING_REGEX='\S   \S SynPS/2 Synaptics TouchPad\s+id=[1-9][0-9]*\s+\[slave\s+pointer\s+\([0-9]+\)\]'
typeset -r DEVICE_MONIKER='TouchPad'

source "${HOME}/etc/xinput_device-toggle.source.sh"

